interface Props {
    value: string;
    index: number;
    onPress: (index: number) => void;
    isFlipped: boolean;
    isMatched: boolean;
}

export function Card() {
    // touchableOpacity
    // renderização condicional para mostrar o value ou a parte de trás do card
}